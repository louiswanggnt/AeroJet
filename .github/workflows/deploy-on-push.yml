name: Deploy on Push

on:
  push:
    branches: [main]
    paths:
      - 'data/promotions/**'
      - 'public/**'
      - 'locales/**'
      - 'app/**'
      - 'components/**'
      - 'config/**'
      - 'i18n/**'
      - 'middleware.ts'
      - 'next.config.ts'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - '.github/workflows/deploy-on-push.yml'
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  validate-build-deploy:
    name: Validate · Build · SFTP Upload
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write   # 允許 commit active.json / archive.json 回 repo

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Upgrade npm (match local lockfile generator)
        run: npm install -g npm@latest

      - name: Install dependencies
        run: npm ci

      - name: Validate promotions schedule
        run: npm run validate:promotions

      - name: Build active.json / archive.json
        run: npm run build:active

      - name: Generate llms.txt
        run: npm run generate:llms

      - name: Lint
        run: npm run lint

      - name: Build Next.js (static export)
        env:
          NEXT_PUBLIC_SITE_URL: ${{ secrets.NEXT_PUBLIC_SITE_URL || 'https://www.auvita.tw' }}
        run: npm run build

      - name: Verify out/ exists
        run: |
          test -d out || (echo "::error::out/ not generated"; exit 1)
          echo "out/ size: $(du -sh out | cut -f1)"
          ls -la out/ | head -20

      - name: Upload to auvita.tw via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_HOST }}
          port: ${{ secrets.FTP_PORT || 21 }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          local-dir: ./out/
          server-dir: ${{ secrets.FTP_REMOTE_PATH || '/' }}
          log-level: minimal
          # 第一次部署可改 dangerous-clean-slate: true（會清空遠端再上傳）
          # 之後增量同步，靠 .ftp-deploy-sync-state.json（remote 自動建立）追蹤

      - name: Commit auto-generated active/archive (if changed)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/promotions/active.json data/promotions/archive.json public/llms.txt
          if git diff --cached --quiet; then
            echo "No auto-generated changes to commit"
          else
            git commit -m "chore(promotions): auto-build active/archive [skip ci]"
            git push origin HEAD:main
          fi

      - name: Notify success
        if: success()
        run: echo "::notice title=Deploy Success::Pushed to ${{ secrets.SFTP_HOST }}:${{ secrets.SFTP_REMOTE_PATH }}"

      - name: Notify failure
        if: failure()
        run: echo "::error title=Deploy Failed::Check workflow logs"
